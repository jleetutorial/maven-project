pipeline {
    agent any
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving code...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}
