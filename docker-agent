pipeline {
    agent none
    stages {


        stage('maven') {
              agent {
        docker { image 'maven:3.6.0-jdk-11-slim' }
    }
            steps {
                sh '''
                mvn clean install package
                '''
            }
        }


         stage('git') {
              agent {
        docker { image 'git:latest' }
    }
            steps {
                sh '''
               git branch
                '''
            }
        }

    }
}
